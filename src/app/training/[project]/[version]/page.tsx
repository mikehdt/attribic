import { TrainingPageBody } from '../../components/training-page-body';

/** A saved project at a specific version — `/training/{slug}/v{n}`. */
export default function TrainingProjectVersionPage() {
  return <TrainingPageBody />;
}
