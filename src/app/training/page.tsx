import { TrainingPageBody } from './components/training-page-body';

/** Unsaved training config — no project has been loaded or saved yet. */
export default function TrainingPage() {
  return <TrainingPageBody />;
}
